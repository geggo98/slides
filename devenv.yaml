inputs:
  # Pinned to the release matching the devenv CLI in use; see AGENTS.md.
  devenv:
    url: github:cachix/devenv/v2.2.2?dir=src/modules
  git-hooks:
    url: github:cachix/git-hooks.nix
    inputs:
      nixpkgs:
        follows: nixpkgs
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
